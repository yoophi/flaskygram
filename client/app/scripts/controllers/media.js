'use strict';

/**
 * @ngdoc function
 * @name clientApp.controller:AboutCtrl
 * @description
 * # AboutCtrl
 * Controller of the clientApp
 */
angular.module('clientApp')
  .controller('MediaCtrl', ['$scope', '$log', function ($scope, $log) {
    $log.debug('MediaCtrl');
  }]);
